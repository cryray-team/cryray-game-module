//-' /////////////////////////////////////////////////////////////////////
//-' OldSerpskiStalker
//-' CryRay Engine x64
//-' Debug: Measurement of memory spent on a call
//-' /////////////////////////////////////////////////////////////////////

#pragma once

namespace MeasureMemory
{
    //template <typename T>
    //size_t calculateSize(const T& arg)
    //{
    //    return sizeof(arg);
    //}

    //template <typename T, typename... Args>
    //size_t calculateSize(const T& arg, const Args&... args)
    //{
    //    return sizeof(arg) + calculateSize(args...);
    //}

    //// Функция для замера памяти
    //template <typename... Args>
    //void measureMemory(const Args&... args)
    //{
    //    size_t totalSize = calculateSize(args...);
    //    Msg("~ Memory consumed: %lu", totalSize);
    //}
} // namespace